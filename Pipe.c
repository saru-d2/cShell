#include "header.h"
#include "JobStruct.h"
char *cmd, *breaks[1000], *par[1000];

bool Pipe(char *line, char *home_dir, job jobArr[], int *jobIterPtr)
{
    printf(TANGO_RED "in Pipe\n" TANGO_WHITE);
    return true;
}